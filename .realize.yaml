settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: clean-arch-demo
  path: .
  commands:
    fmt:
      status: true
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignore:
      paths:
      - .git
      - .realize
      - vendor
